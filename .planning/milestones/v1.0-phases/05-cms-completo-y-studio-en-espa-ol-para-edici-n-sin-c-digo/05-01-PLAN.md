---
phase: 05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/schemas/site-settings.ts
  - src/sanity/sanity.config.ts
  - src/sanity/structure.ts
  - package.json
autonomous: true
requirements: [CMS5-01, CMS5-02]

must_haves:
  truths:
    - "siteSettings schema has servicios, valores, metricas, nosotros fields with Spanish titles and descriptions"
    - "siteSettings schema uses fieldGroups with 5 tabs (general, homepage, nosotros, contacto, navegacion)"
    - "Studio UI chrome is in Spanish via @sanity/locale-es-es plugin"
    - "Studio sidebar shows organized sections via custom Structure Builder"
    - "All icono fields use options.list with Spanish display titles (no free text)"
  artifacts:
    - path: "src/sanity/schemas/site-settings.ts"
      provides: "Extended siteSettings schema with all content arrays and fieldGroups"
      contains: "servicios"
    - path: "src/sanity/structure.ts"
      provides: "Custom Structure Builder sidebar resolver"
      exports: ["myStructure"]
    - path: "src/sanity/sanity.config.ts"
      provides: "Studio config with esESLocale and custom structure"
      contains: "esESLocale"
  key_links:
    - from: "src/sanity/sanity.config.ts"
      to: "src/sanity/structure.ts"
      via: "import myStructure"
      pattern: "import.*myStructure.*from.*structure"
    - from: "src/sanity/sanity.config.ts"
      to: "@sanity/locale-es-es"
      via: "plugin registration"
      pattern: "esESLocale\\(\\)"
---

<objective>
Extend the siteSettings Sanity schema with homepage content arrays (servicios, valores, metricas) and nosotros page fields, add fieldGroups (tabs) for organized editing, install the Spanish locale plugin, and configure a custom Structure Builder sidebar for non-technical editors.

Purpose: Make all currently-hardcoded content editable from Sanity Studio, with a Spanish-language UI organized into logical tabs.
Output: Extended schema, Structure Builder resolver, updated Studio config with locale plugin.
</objective>

<execution_context>
@/Users/hugoyupanqui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hugoyupanqui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-RESEARCH.md
@.planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install locale plugin and extend siteSettings schema with fieldGroups and content arrays</name>
  <files>package.json, src/sanity/schemas/site-settings.ts</files>
  <read_first>
    - src/sanity/schemas/site-settings.ts (current schema — must preserve existing fields)
    - src/sanity/schemas/index.ts (schema registration — no changes needed but verify siteSettings is already registered)
    - package.json (verify current sanity version compatibility with locale plugin)
    - .planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-RESEARCH.md (complete schema example in Code Examples section)
  </read_first>
  <action>
1. Run `npm install @sanity/locale-es-es` to add the Spanish locale plugin.

2. Rewrite `src/sanity/schemas/site-settings.ts` with the following changes (preserve ALL existing fields — siteName, logo, whatsappNumber, email, address, navLinks, socialLinks, footerText):

   a. Add `groups` array to the defineType:
      ```
      groups: [
        { name: 'general', title: 'Informacion General', default: true },
        { name: 'homepage', title: 'Pagina de Inicio' },
        { name: 'nosotros', title: 'Pagina Nosotros' },
        { name: 'contacto', title: 'Contacto y Redes' },
        { name: 'navegacion', title: 'Menu y Footer' },
      ]
      ```

   b. Assign existing fields to groups:
      - siteName, logo -> group: 'general'
      - whatsappNumber, email, address, socialLinks -> group: 'contacto'
      - navLinks, footerText -> group: 'navegacion'

   c. Add Spanish `title` and `description` to ALL existing fields that lack them:
      - whatsappNumber: description: 'Numero completo con codigo de pais. Ejemplo: 51987654321 (Peru = 51).'
      - email: description: 'Email principal de contacto de la empresa.'
      - address: description: 'Direccion fisica de la oficina o laboratorio.'
      - navLinks: description: 'Elementos del menu principal. El orden aqui determina el orden en el menu.'
      - navLinks children: label -> title: 'Etiqueta', description: 'Texto visible en el menu. Ejemplo: "Nosotros"'; href -> title: 'Enlace', description: 'Ruta interna. Ejemplo: /nosotros'
      - socialLinks children: platform -> title: 'Red Social', options: { list: ['Facebook', 'Instagram', 'LinkedIn', 'YouTube'] }; url -> title: 'URL del Perfil'
      - footerText: description: 'Texto de copyright. Ejemplo: "2025 Testing Calibrations S.A.C. Todos los derechos reservados."'
      - siteName: description: 'Nombre que aparece en el navegador y en el encabezado del sitio.'
      - logo: description: 'Logo principal del sitio. Formatos recomendados: PNG o SVG con fondo transparente.'

   d. Add NEW array fields for homepage content (group: 'homepage'):
      - `servicios` (array of objects): each with titulo (string, required), descripcion (text, rows: 3), icono (string with options.list: [{title:'Llave inglesa',value:'wrench'},{title:'Check (verificacion)',value:'check-circle'},{title:'Escudo',value:'shield'},{title:'Engranaje',value:'settings'},{title:'Rayo',value:'zap'}])
        - Field description: 'Tarjetas de la seccion "Nuestros Servicios" en la pagina de inicio.'
      - `valores` (array of objects): each with titulo (string, required), descripcion (text, rows: 3), icono (string with options.list: [{title:'Premio / Trofeo',value:'award'},{title:'Reloj',value:'clock'},{title:'Personas / Equipo',value:'users'},{title:'Escudo',value:'shield'},{title:'Check',value:'check-circle'}])
        - Field description: 'Tarjetas de la seccion "Por que elegirnos?" en la pagina de inicio.'
      - `metricas` (array of objects): each with valor (number, required, description: 'El numero que se anima (sin el simbolo +). Ejemplo: 200'), sufijo (string, description: 'Texto despues del numero. Ejemplo: "+" o "%"'), etiqueta (string, required, description: 'Descripcion debajo del numero. Ejemplo: "Clientes atendidos"')
        - Field description: 'Numeros animados que aparecen en la seccion de estadisticas.'

   e. Add NEW fields for nosotros page (group: 'nosotros'):
      - `nosotrosHistoria` (text, rows: 5): title: 'Historia de la Empresa', description: 'Parrafo de presentacion de la empresa en la pagina Nosotros.'
      - `nosotrosMision` (text, rows: 3): title: 'Mision'
      - `nosotrosVision` (text, rows: 3): title: 'Vision'
      - `nosotrosValores` (array of objects): title: 'Valores de la Empresa', description: 'Tarjetas de valores en la pagina Nosotros.' Each with titulo (string, required), descripcion (text, rows: 3), icono (string with options.list: [{title:'Check / Precision',value:'check-circle'},{title:'Escudo / Integridad',value:'shield'},{title:'Personas / Compromiso',value:'users'}])

   f. Keep the singleton pattern: `Object.assign(siteSettingsBase, { __experimental_actions: ['update', 'publish'] })`

   g. Use `defineField` and `defineArrayMember` for all fields. Array children use `defineArrayMember` (not `defineField` with name).
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && grep -q "servicios" src/sanity/schemas/site-settings.ts && grep -q "valores" src/sanity/schemas/site-settings.ts && grep -q "metricas" src/sanity/schemas/site-settings.ts && grep -q "nosotrosHistoria" src/sanity/schemas/site-settings.ts && grep -q "nosotrosValores" src/sanity/schemas/site-settings.ts && grep -q "groups" src/sanity/schemas/site-settings.ts && grep -q "locale-es-es" package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/sanity/schemas/site-settings.ts contains `groups: [` with 5 group definitions (general, homepage, nosotros, contacto, navegacion)
    - src/sanity/schemas/site-settings.ts contains `name: 'servicios'` with `group: 'homepage'`
    - src/sanity/schemas/site-settings.ts contains `name: 'valores'` with `group: 'homepage'`
    - src/sanity/schemas/site-settings.ts contains `name: 'metricas'` with `group: 'homepage'`
    - src/sanity/schemas/site-settings.ts contains `name: 'nosotrosHistoria'` with `group: 'nosotros'`
    - src/sanity/schemas/site-settings.ts contains `name: 'nosotrosValores'` with `group: 'nosotros'`
    - src/sanity/schemas/site-settings.ts contains `options: { list:` inside icono fields (no free-text icon input)
    - src/sanity/schemas/site-settings.ts contains `__experimental_actions: ['update', 'publish']`
    - package.json contains `@sanity/locale-es-es`
    - All existing fields (siteName, logo, whatsappNumber, email, address, navLinks, socialLinks, footerText) are still present
  </acceptance_criteria>
  <done>siteSettings schema has 5 fieldGroups, all homepage content arrays (servicios, valores, metricas), nosotros fields, Spanish descriptions on every field, constrained icon lists, and singleton pattern preserved. Locale plugin installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Structure Builder and update sanity.config.ts with locale and custom structure</name>
  <files>src/sanity/structure.ts, src/sanity/sanity.config.ts</files>
  <read_first>
    - src/sanity/sanity.config.ts (current config — must preserve projectId, dataset, basePath, schema)
    - src/sanity/schemas/index.ts (verify schema type names for Structure Builder: equipo, post, banner, pagina, siteSettings, reclamo)
    - .planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-RESEARCH.md (Pattern 4 — Structure Builder example)
    - .planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-UI-SPEC.md (Sidebar Structure section — exact order and labels)
  </read_first>
  <action>
1. Create NEW file `src/sanity/structure.ts`:

```typescript
import { type StructureResolver } from 'sanity/structure'

export const myStructure: StructureResolver = (S) =>
  S.list()
    .title('Contenido')
    .items([
      S.listItem()
        .title('Configuracion General')
        .id('siteSettings')
        .child(
          S.document()
            .schemaType('siteSettings')
            .documentId('siteSettings')
        ),
      S.divider(),
      S.documentTypeListItem('equipo').title('Equipos'),
      S.documentTypeListItem('banner').title('Banners Hero'),
      S.documentTypeListItem('post').title('Articulos del Blog'),
      S.documentTypeListItem('pagina').title('Paginas'),
      S.divider(),
      S.documentTypeListItem('reclamo').title('Reclamos Recibidos'),
    ])
```

2. Update `src/sanity/sanity.config.ts`:
   - Add import: `import { esESLocale } from '@sanity/locale-es-es'`
   - Add import: `import { myStructure } from './structure'`
   - Change `plugins` array from `[structureTool(), visionTool()]` to `[structureTool({ structure: myStructure }), visionTool(), esESLocale()]`
   - Keep ALL other config unchanged (name, title, projectId, dataset, basePath, schema)

Final sanity.config.ts should be:
```typescript
import { defineConfig } from 'sanity'
import { structureTool } from 'sanity/structure'
import { visionTool } from '@sanity/vision'
import { esESLocale } from '@sanity/locale-es-es'
import { schemaTypes } from './schemas'
import { myStructure } from './structure'

export default defineConfig({
  name: 'testing-calibrations',
  title: 'Testing Calibrations S.A.C.',
  projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID!,
  dataset: process.env.NEXT_PUBLIC_SANITY_DATASET ?? 'production',
  basePath: '/studio',
  plugins: [structureTool({ structure: myStructure }), visionTool(), esESLocale()],
  schema: { types: schemaTypes },
})
```
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && grep -q "esESLocale" src/sanity/sanity.config.ts && grep -q "myStructure" src/sanity/sanity.config.ts && grep -q "myStructure" src/sanity/structure.ts && grep -q "Configuracion General" src/sanity/structure.ts && grep -q "Reclamos Recibidos" src/sanity/structure.ts && npx tsc --noEmit --skipLibCheck 2>&1 | head -20 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/sanity/structure.ts exists and exports `myStructure`
    - src/sanity/structure.ts contains `S.listItem().title('Configuracion General').id('siteSettings')`
    - src/sanity/structure.ts contains `.documentId('siteSettings')` (singleton pattern)
    - src/sanity/structure.ts contains `S.documentTypeListItem('equipo').title('Equipos')`
    - src/sanity/structure.ts contains `S.documentTypeListItem('reclamo').title('Reclamos Recibidos')`
    - src/sanity/structure.ts contains two `S.divider()` calls
    - src/sanity/sanity.config.ts contains `import { esESLocale } from '@sanity/locale-es-es'`
    - src/sanity/sanity.config.ts contains `import { myStructure } from './structure'`
    - src/sanity/sanity.config.ts contains `structureTool({ structure: myStructure })`
    - src/sanity/sanity.config.ts contains `esESLocale()` in plugins array
    - TypeScript compilation passes (npx tsc --noEmit --skipLibCheck exits 0)
  </acceptance_criteria>
  <done>Studio has a custom sidebar with Spanish labels for all document types, siteSettings renders as a singleton edit pane, and the locale plugin translates Studio chrome to Spanish.</done>
</task>

</tasks>

<verification>
- `grep -c "group:" src/sanity/schemas/site-settings.ts` returns >= 13 (every field has a group)
- `grep "options.*list" src/sanity/schemas/site-settings.ts` returns matches for all 3 icono fields
- `npm list @sanity/locale-es-es` returns a version
- `npx tsc --noEmit --skipLibCheck` exits 0
</verification>

<success_criteria>
- siteSettings schema has 5 tab groups, 3 homepage content arrays, 4 nosotros fields
- All icono fields constrained to options.list (no free-text)
- All fields have Spanish title and description
- Structure Builder sidebar matches UI-SPEC order exactly
- esESLocale plugin registered in sanity.config.ts
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-01-SUMMARY.md`
</output>
