---
phase: 04-seo-and-launch
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/api/contact/route.ts
  - src/app/api/reclamo/route.ts
autonomous: false
requirements: [SEO-01, SEO-02, SEO-03]

user_setup:
  - service: resend
    why: "Email delivery from verified production domain"
    env_vars:
      - name: RESEND_FROM_EMAIL
        source: "Set to 'Testing Calibrations <noreply@testingcalibrations.com.pe>' after domain verification in Resend Dashboard -> Domains"
    dashboard_config:
      - task: "Add and verify sender domain (testingcalibrations.com.pe)"
        location: "Resend Dashboard -> Domains -> Add Domain -> add SPF and DKIM DNS records"
  - service: sanity
    why: "Studio CORS for production URL"
    dashboard_config:
      - task: "Add production CORS origin with credentials"
        location: "manage.sanity.io -> Project -> Settings -> API -> CORS Origins -> Add https://testingcalibrations.com.pe with 'Allow credentials' checked"

must_haves:
  truths:
    - "Contact form email uses a configurable from address via RESEND_FROM_EMAIL env var, not hardcoded onboarding@resend.dev"
    - "Reclamo form email uses the same configurable from address"
    - "Contact form POST on production delivers email successfully"
    - "Sanity Studio login works at /studio on the production URL"
    - "WhatsApp button opens correctly on a real mobile device"
    - "All required env vars are present on the production server"
  artifacts:
    - path: "src/app/api/contact/route.ts"
      provides: "Contact email with configurable sender"
      contains: "RESEND_FROM_EMAIL"
    - path: "src/app/api/reclamo/route.ts"
      provides: "Reclamo email with configurable sender"
      contains: "RESEND_FROM_EMAIL"
  key_links:
    - from: "src/app/api/contact/route.ts"
      to: "process.env.RESEND_FROM_EMAIL"
      via: "env var for sender address"
      pattern: "process\\.env\\.RESEND_FROM_EMAIL"
    - from: "src/app/api/reclamo/route.ts"
      to: "process.env.RESEND_FROM_EMAIL"
      via: "env var for sender address"
      pattern: "process\\.env\\.RESEND_FROM_EMAIL"
---

<objective>
Production hardening: replace hardcoded Resend sandbox email with configurable env var, then verify all production services work end-to-end.

Purpose: ROADMAP success criterion 4 requires working production email delivery from a verified domain, Sanity Studio CORS on production, and env var audit. RESEARCH.md identifies these as critical pitfalls (Pitfall 2, 3, 4). The contact and reclamo API routes currently use `onboarding@resend.dev` with TODO comments.
Output: Updated API routes using `RESEND_FROM_EMAIL` env var, and human-verified production checklist.
</objective>

<execution_context>
@/Users/hugoyupanqui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hugoyupanqui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-and-launch/04-RESEARCH.md

<interfaces>
<!-- Existing API routes with hardcoded from address -->
From src/app/api/contact/route.ts (line 14):
```typescript
from: 'onboarding@resend.dev', // TODO: Replace with verified domain before production
```

From src/app/api/reclamo/route.ts (line 47):
```typescript
from: 'onboarding@resend.dev', // TODO: Replace with verified domain
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded Resend from address with RESEND_FROM_EMAIL env var</name>
  <files>src/app/api/contact/route.ts, src/app/api/reclamo/route.ts</files>
  <read_first>
    - src/app/api/contact/route.ts
    - src/app/api/reclamo/route.ts
  </read_first>
  <action>
In both API route files, replace the hardcoded `from: 'onboarding@resend.dev'` with:

```typescript
from: process.env.RESEND_FROM_EMAIL ?? 'onboarding@resend.dev',
```

This allows production to use a verified sender domain via the `RESEND_FROM_EMAIL` env var while keeping the Resend sandbox as a development fallback.

**src/app/api/contact/route.ts** — Find the line:
```typescript
from: 'onboarding@resend.dev', // TODO: Replace with verified domain before production
```
Replace with:
```typescript
from: process.env.RESEND_FROM_EMAIL ?? 'onboarding@resend.dev',
```

**src/app/api/reclamo/route.ts** — Find the line:
```typescript
from: 'onboarding@resend.dev', // TODO: Replace with verified domain
```
Replace with:
```typescript
from: process.env.RESEND_FROM_EMAIL ?? 'onboarding@resend.dev',
```

Remove the TODO comments — the TODO is now resolved by the env var approach.
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && grep -n "RESEND_FROM_EMAIL" src/app/api/contact/route.ts src/app/api/reclamo/route.ts && ! grep -n "onboarding@resend.dev" src/app/api/contact/route.ts src/app/api/reclamo/route.ts | grep -v "RESEND_FROM_EMAIL" | grep -c "onboarding" | grep -q "^0$" && echo "PASS" || echo "Check grep output"</automated>
  </verify>
  <done>Both API routes use `process.env.RESEND_FROM_EMAIL` with sandbox fallback. No hardcoded production sender address. No TODO comments remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production services verification checklist</name>
  <files>N/A — verification only, no file changes</files>
  <action>
Human verifies all production services work end-to-end. This covers ROADMAP Phase 4 success criterion 4 and the manual-only verifications from VALIDATION.md.

**Pre-requisites (user must complete before verifying):**
1. In Resend Dashboard -> Domains: add `testingcalibrations.com.pe`, add SPF and DKIM DNS records, wait for verification
2. In manage.sanity.io -> Settings -> API -> CORS Origins: add `https://testingcalibrations.com.pe` with "Allow credentials" checked
3. On the production VPS, ensure these env vars are set:
   - `NEXT_PUBLIC_SANITY_PROJECT_ID`
   - `NEXT_PUBLIC_SANITY_DATASET`
   - `NEXT_PUBLIC_SITE_URL`
   - `RESEND_API_KEY`
   - `RESEND_FROM_EMAIL` (e.g., `Testing Calibrations <noreply@testingcalibrations.com.pe>`)
   - `SANITY_WRITE_TOKEN`
4. Rebuild and restart the app on production (`npm run build && npm start`)
  </action>
  <verify>
    <automated>MISSING — manual-only verification (production email delivery, Studio CORS, WhatsApp mobile, env var audit)</automated>
  </verify>
  <what-built>
    Email sender address is now configurable via RESEND_FROM_EMAIL env var. This task verifies all production services work end-to-end.
  </what-built>
  <how-to-verify>
    1. **Email delivery**: Go to production /contacto, fill out the contact form, submit. Verify the company email receives the notification (not in spam).
    2. **Reclamo email**: Go to production /libro-de-reclamaciones, fill out the form, submit. Verify the company email receives the notification.
    3. **Sanity Studio login**: Navigate to production /studio, login with Sanity credentials. Verify the Studio loads and shows content.
    4. **WhatsApp button (mobile)**: Open the production site on a real mobile device. Tap the WhatsApp floating button. Verify WhatsApp opens with the pre-filled message.
    5. **Env var audit**: On the VPS, run `printenv | grep -E "NEXT_PUBLIC_SANITY|RESEND|SANITY_WRITE"` and confirm all 6 variables are set (values redacted is fine, just confirm they exist).
  </how-to-verify>
  <done>All 5 production checks pass: email delivery from verified domain, reclamo email delivery, Studio login on production, WhatsApp on mobile, env vars present.</done>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which checks failed and what happened.</resume-signal>
</task>

</tasks>

<verification>
- `grep "RESEND_FROM_EMAIL" src/app/api/contact/route.ts src/app/api/reclamo/route.ts` returns matches in both files
- `npx tsc --noEmit` passes
- Manual verification of production services (Task 2 checkpoint)
</verification>

<success_criteria>
- Both API routes use RESEND_FROM_EMAIL env var instead of hardcoded onboarding@resend.dev
- Contact form delivers email from production domain (verified manually)
- Sanity Studio login works on production URL (verified manually)
- WhatsApp button works on mobile device (verified manually)
- All required env vars present on production server (verified manually)
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-and-launch/04-03-SUMMARY.md`
</output>
