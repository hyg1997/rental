---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(site)/layout.tsx
  - src/app/(site)/page.tsx
  - src/sanity/client.ts
  - src/lib/utils.ts
  - components.json
  - .env.local
  - playwright.config.ts
  - tests/smoke.spec.ts
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "Next.js 15 dev server starts without errors on localhost:3000"
    - "Tailwind v4 custom brand colors resolve (bg-brand-bg, bg-brand-surface, text-brand-red, text-brand-yellow, text-brand-text)"
    - "shadcn/ui Sheet and Button components are installed and importable"
    - "Sanity client can be imported from @/sanity/client without errors"
    - "Placeholder page renders at localhost:3000 with dark background"
    - "Playwright smoke test file exists and can be invoked (even if tests fail at this point)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-first theme with OKLCH brand palette"
      contains: "@theme inline"
    - path: "src/sanity/client.ts"
      provides: "Sanity client configured with env vars"
      contains: "createClient"
    - path: "src/app/(site)/layout.tsx"
      provides: "Site route group layout shell (will receive Navbar/Footer in Plan 03)"
    - path: "playwright.config.ts"
      provides: "Playwright config with 360px viewport project"
      contains: "360"
    - path: "tests/smoke.spec.ts"
      provides: "Smoke test stubs for CMS-02, UX-01, UX-02, UX-03"
  key_links:
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "src/sanity/client.ts"
      to: ".env.local"
      via: "environment variables"
      pattern: "NEXT_PUBLIC_SANITY_PROJECT_ID"
---

<objective>
Scaffold the Next.js 15 project with TypeScript, Tailwind v4 CSS-first theming, shadcn/ui, Sanity client, and Playwright test infrastructure. This creates the deployable skeleton that all subsequent plans build on.

Purpose: Every other plan depends on a working Next.js project with the brand theme, component library, CMS client, and test framework in place.
Output: A running dev server at localhost:3000 showing a dark-themed placeholder page, with shadcn/ui components available, Sanity client configured, and Playwright ready for smoke tests.
</objective>

<execution_context>
@/Users/hugoyupanqui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hugoyupanqui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with Tailwind v4, shadcn/ui, and Sanity client</name>
  <files>
    package.json, tsconfig.json, next.config.ts, src/app/globals.css, src/app/layout.tsx,
    src/app/(site)/layout.tsx, src/app/(site)/page.tsx, src/sanity/client.ts, src/lib/utils.ts,
    components.json, .env.local
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>
    1. Create Next.js 15 project:
       ```
       npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
       ```
       If the directory is not empty, run in a temp dir and move files. The project name should be "testing-calibrations".

    2. Install dependencies:
       ```
       npm install next-sanity @sanity/client @sanity/image-url groq @portabletext/react next-themes
       npx shadcn@latest init --defaults
       npx shadcn@latest add sheet button
       ```

    3. Configure `next.config.ts` for VPS deployment (full Next.js, no static export):
       ```typescript
       import type { NextConfig } from 'next'
       const nextConfig: NextConfig = {}
       export default nextConfig
       ```
       NOTE: VPS path per research recommendation. If client confirms shared hosting later, add `output: 'export'`, `trailingSlash: true`, `images: { unoptimized: true }`.

    4. Replace `src/app/globals.css` with Tailwind v4 CSS-first theme:
       ```css
       @import "tailwindcss";
       @import "tw-animate-css";

       @custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *));

       @theme inline {
         --color-brand-bg: oklch(0.12 0.01 260);
         --color-brand-surface: oklch(0.18 0.01 260);
         --color-brand-red: oklch(0.55 0.22 25);
         --color-brand-yellow: oklch(0.82 0.18 85);
         --color-brand-text: oklch(0.96 0.01 260);

         --font-heading: 'Inter', sans-serif;
         --font-body: 'Inter', sans-serif;
       }
       ```
       IMPORTANT: Keep any shadcn/ui CSS variables that `npx shadcn@latest init` added (the `--radius`, `--background`, `--foreground`, etc. variables). Merge them — do not delete. The brand palette goes INSIDE `@theme inline`, shadcn variables stay where shadcn put them.

    5. Update `src/app/layout.tsx` (root layout):
       - Import Inter from `next/font/google` with weights [400, 700]
       - Apply Inter className to `<html>` tag
       - Set `<html lang="es">` (Spanish site)
       - Import `globals.css`
       - Set metadata: title "Testing Calibrations S.A.C." description "Calibración de analizadores de gases vehiculares"
       - Do NOT add Navbar/Footer here — those go in the (site) layout in Plan 03

    6. Create `src/app/(site)/layout.tsx` (site route group layout):
       ```typescript
       export default function SiteLayout({ children }: { children: React.ReactNode }) {
         return (
           <>
             {/* Navbar and Footer will be added in Plan 01-03 */}
             <main>{children}</main>
           </>
         )
       }
       ```

    7. Create `src/app/(site)/page.tsx` (placeholder homepage):
       ```typescript
       export default function HomePage() {
         return (
           <div className="min-h-screen flex flex-col items-center justify-center bg-brand-bg text-brand-text px-4">
             <h1 className="text-[28px] font-bold leading-[1.15] text-center">
               Bienvenido a Testing Calibrations S.A.C.
             </h1>
             <p className="mt-4 text-base leading-relaxed text-center">
               Sitio en construcción. Vuelve pronto.
             </p>
           </div>
         )
       }
       ```

    8. Create `src/sanity/client.ts`:
       ```typescript
       import { createClient } from '@sanity/client'

       export const client = createClient({
         projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID!,
         dataset: process.env.NEXT_PUBLIC_SANITY_DATASET ?? 'production',
         apiVersion: '2024-01-01',
         useCdn: process.env.NODE_ENV === 'production',
       })
       ```

    9. Create `.env.local`:
       ```
       NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here
       NEXT_PUBLIC_SANITY_DATASET=production
       ```

    10. Verify `src/lib/utils.ts` exists with `cn()` helper (created by shadcn init). If not, create it:
        ```typescript
        import { clsx, type ClassValue } from "clsx"
        import { twMerge } from "tailwind-merge"
        export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
        ```
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" in dependencies
    - package.json contains "@sanity/client" in dependencies
    - package.json contains "next-sanity" in dependencies
    - src/app/globals.css contains "@theme inline"
    - src/app/globals.css contains "--color-brand-bg: oklch(0.12 0.01 260)"
    - src/app/globals.css contains "--color-brand-red: oklch(0.55 0.22 25)"
    - src/app/globals.css contains "--color-brand-yellow: oklch(0.82 0.18 85)"
    - src/app/globals.css contains "--color-brand-text: oklch(0.96 0.01 260)"
    - src/app/layout.tsx contains 'lang="es"'
    - src/app/layout.tsx contains "Inter" (font import)
    - src/app/(site)/page.tsx contains "Bienvenido a Testing Calibrations S.A.C."
    - src/app/(site)/page.tsx contains "bg-brand-bg"
    - src/app/(site)/layout.tsx exists
    - src/sanity/client.ts contains "createClient" imported from "@sanity/client"
    - src/sanity/client.ts contains "NEXT_PUBLIC_SANITY_PROJECT_ID"
    - src/components/ui/sheet.tsx exists (shadcn Sheet installed)
    - src/components/ui/button.tsx exists (shadcn Button installed)
    - .env.local contains "NEXT_PUBLIC_SANITY_PROJECT_ID"
    - npm run build exits without error
  </acceptance_criteria>
  <done>Next.js 15 project builds successfully with Tailwind v4 brand theme, shadcn/ui Sheet+Button, Sanity client, and placeholder homepage showing dark background with Spanish text.</done>
</task>

<task type="auto">
  <name>Task 2: Set up Playwright test infrastructure with smoke test stubs</name>
  <files>
    playwright.config.ts, tests/smoke.spec.ts, package.json
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md
    package.json
  </read_first>
  <action>
    1. Install Playwright:
       ```
       npm install -D @playwright/test
       npx playwright install chromium
       ```

    2. Create `playwright.config.ts`:
       ```typescript
       import { defineConfig, devices } from '@playwright/test'

       export default defineConfig({
         testDir: './tests',
         fullyParallel: true,
         forbidOnly: !!process.env.CI,
         retries: process.env.CI ? 2 : 0,
         workers: process.env.CI ? 1 : undefined,
         reporter: 'html',
         use: {
           baseURL: 'http://localhost:3000',
           trace: 'on-first-retry',
         },
         projects: [
           {
             name: 'chromium',
             use: { ...devices['Desktop Chrome'] },
           },
           {
             name: 'mobile-360',
             use: {
               viewport: { width: 360, height: 640 },
               userAgent: 'Mozilla/5.0 (Linux; Android 10) AppleWebKit/537.36',
             },
           },
         ],
         webServer: {
           command: 'npm run dev',
           url: 'http://localhost:3000',
           reuseExistingServer: !process.env.CI,
         },
       })
       ```

    3. Create `tests/smoke.spec.ts` with stubs for all Phase 1 requirements:
       ```typescript
       import { test, expect } from '@playwright/test'

       test.describe('Phase 1 Smoke Tests', () => {
         test('homepage renders placeholder', async ({ page }) => {
           await page.goto('/')
           await expect(page.locator('h1')).toContainText('Bienvenido a Testing Calibrations S.A.C.')
         })

         test('studio loads', async ({ page }) => {
           // CMS-02: Sanity Studio accessible at /studio
           await page.goto('/studio')
           // Studio renders a root element — check it loads without 404
           await expect(page).not.toHaveTitle(/404/)
         })

         test('360px mobile - no horizontal scroll', async ({ page }) => {
           // UX-01: Responsive at 360px
           await page.setViewportSize({ width: 360, height: 640 })
           await page.goto('/')
           const body = page.locator('body')
           const scrollWidth = await body.evaluate(el => el.scrollWidth)
           const clientWidth = await body.evaluate(el => el.clientWidth)
           expect(scrollWidth).toBeLessThanOrEqual(clientWidth)
         })

         test('dark theme - brand background visible', async ({ page }) => {
           // UX-02: Dark theme with brand colors
           await page.goto('/')
           // The page should have a dark background (brand-bg)
           const bgColor = await page.locator('div.bg-brand-bg').first().evaluate(
             el => getComputedStyle(el).backgroundColor
           )
           // brand-bg is oklch(0.12 0.01 260) — very dark, RGB values should be low
           expect(bgColor).toBeTruthy()
         })

         test('hamburger menu - mobile nav opens', async ({ page }) => {
           // UX-03: Hamburger menu on mobile
           await page.setViewportSize({ width: 360, height: 640 })
           await page.goto('/')
           // Will be implemented in Plan 01-03 — stub checks for hamburger button
           const menuButton = page.locator('button[aria-label="Abrir menú"]')
           // This test will fail until Plan 01-03 adds the Navbar
           // Uncomment after Plan 01-03:
           // await expect(menuButton).toBeVisible()
           // await menuButton.click()
           // await expect(page.locator('[data-state="open"]')).toBeVisible()
         })
       })
       ```

    4. Add script to `package.json`:
       ```json
       "test:e2e": "npx playwright test",
       "test:smoke": "npx playwright test tests/smoke.spec.ts --project=chromium"
       ```
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && npx playwright test tests/smoke.spec.ts --project=chromium -g "homepage renders placeholder" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts contains "mobile-360"
    - playwright.config.ts contains "width: 360"
    - tests/smoke.spec.ts contains "studio loads"
    - tests/smoke.spec.ts contains "360px mobile"
    - tests/smoke.spec.ts contains "dark theme"
    - tests/smoke.spec.ts contains "hamburger menu"
    - tests/smoke.spec.ts contains "Abrir menú"
    - package.json contains "test:e2e" script
    - package.json contains "@playwright/test" in devDependencies
    - npx playwright test tests/smoke.spec.ts --project=chromium -g "homepage renders placeholder" passes
  </acceptance_criteria>
  <done>Playwright installed with 360px mobile viewport project, smoke test stubs exist for all Phase 1 requirements (CMS-02, UX-01, UX-02, UX-03), and the homepage placeholder test passes green.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run dev` starts and localhost:3000 shows the placeholder page with dark background
- `npx playwright test tests/smoke.spec.ts --project=chromium -g "homepage"` passes
- `npx tsc --noEmit` passes
- src/components/ui/sheet.tsx and button.tsx exist (shadcn components)
- Brand colors are defined in globals.css with OKLCH values
</verification>

<success_criteria>
- Next.js 15 project builds and runs
- Tailwind v4 brand palette (dark bg, red/yellow accents) is configured in CSS-first style
- shadcn/ui initialized with Sheet and Button components
- Sanity client configured (connection tested in Plan 02)
- Route group structure: (site) for public pages, studio outside
- Playwright with 360px viewport project and smoke test stubs
- Placeholder page shows "Bienvenido a Testing Calibrations S.A.C." on dark background
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
