---
phase: 05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/sanity/queries/homepage.ts
  - src/components/home/services-section.tsx
  - src/components/home/values-section.tsx
  - src/components/home/metrics-counter.tsx
  - src/app/(site)/page.tsx
autonomous: true
requirements: [CMS5-03]

must_haves:
  truths:
    - "ServicesSection renders from Sanity servicios array prop, not hardcoded JSX"
    - "ValuesSection renders from Sanity valores array prop, not hardcoded JSX"
    - "MetricsSection renders from Sanity metricas array prop, not hardcoded Counter calls"
    - "Homepage fetches servicios, valores, metricas from Sanity via HOMEPAGE_CONTENT_QUERY"
    - "All sections show silent empty state when Sanity returns empty arrays"
  artifacts:
    - path: "src/sanity/queries/homepage.ts"
      provides: "HOMEPAGE_CONTENT_QUERY GROQ query"
      exports: ["HOMEPAGE_CONTENT_QUERY"]
    - path: "src/components/home/services-section.tsx"
      provides: "Prop-driven ServicesSection component"
      contains: "servicios"
    - path: "src/components/home/values-section.tsx"
      provides: "Prop-driven ValuesSection component"
      contains: "valores"
    - path: "src/components/home/metrics-counter.tsx"
      provides: "Prop-driven MetricsSection component"
      contains: "metricas"
    - path: "src/app/(site)/page.tsx"
      provides: "Homepage with Sanity fetch for all sections"
      contains: "HOMEPAGE_CONTENT_QUERY"
  key_links:
    - from: "src/app/(site)/page.tsx"
      to: "src/sanity/queries/homepage.ts"
      via: "import HOMEPAGE_CONTENT_QUERY"
      pattern: "import.*HOMEPAGE_CONTENT_QUERY.*from.*queries/homepage"
    - from: "src/app/(site)/page.tsx"
      to: "src/components/home/services-section.tsx"
      via: "passing servicios prop"
      pattern: "servicios=.*homepageContent"
---

<objective>
Wire the three homepage sections (Services, Values, Metrics) to accept data from Sanity instead of rendering hardcoded content. Create a GROQ query for homepage-specific siteSettings fields and update page.tsx to fetch and pass data as props.

Purpose: Non-technical editors can change services, values, and metrics content from Sanity Studio without code changes.
Output: Prop-driven components, GROQ query, updated homepage server component.
</objective>

<execution_context>
@/Users/hugoyupanqui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hugoyupanqui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-RESEARCH.md
@.planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-UI-SPEC.md
@.planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-01-SUMMARY.md

<interfaces>
<!-- From src/sanity/client.ts -->
import { client } from '@/sanity/client'
<!-- client.fetch(QUERY).catch(() => []) pattern used throughout -->

<!-- From src/sanity/queries/ pattern (all files use this) -->
import { defineQuery } from 'groq'
export const SOME_QUERY = defineQuery(`...`)

<!-- From existing page.tsx pattern -->
const [banners, featuredEquipos] = await Promise.all([
  client.fetch(BANNERS_QUERY).catch(() => []),
  client.fetch(FEATURED_EQUIPOS_QUERY).catch(() => []),
])

<!-- Lucide icon imports currently used in components -->
<!-- services-section: Wrench, CheckCircle, Shield -->
<!-- values-section: Award, Clock, Users -->
<!-- Icon mapping needed: string name -> Lucide component -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GROQ query and refactor all three homepage section components to accept props</name>
  <files>src/sanity/queries/homepage.ts, src/components/home/services-section.tsx, src/components/home/values-section.tsx, src/components/home/metrics-counter.tsx</files>
  <read_first>
    - src/components/home/services-section.tsx (current hardcoded component — need to preserve exact Tailwind classes and layout)
    - src/components/home/values-section.tsx (current hardcoded component — preserve layout)
    - src/components/home/metrics-counter.tsx (current hardcoded component — preserve Counter sub-component and animation logic)
    - src/sanity/queries/site-settings.ts (existing query pattern — use defineQuery from groq)
    - src/sanity/schemas/site-settings.ts (new schema from Plan 01 — field names for GROQ projection)
    - .planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-UI-SPEC.md (empty state design rules)
  </read_first>
  <action>
1. Create NEW file `src/sanity/queries/homepage.ts`:
```typescript
import { defineQuery } from 'groq'

export const HOMEPAGE_CONTENT_QUERY = defineQuery(`
  *[_type == "siteSettings"][0]{
    servicios[]{titulo, descripcion, icono},
    valores[]{titulo, descripcion, icono},
    metricas[]{valor, sufijo, etiqueta}
  }
`)
```

2. Refactor `src/components/home/services-section.tsx`:
   - Add interface: `interface Servicio { titulo: string; descripcion: string; icono: string }`
   - Add interface: `interface ServicesSectionProps { servicios?: Servicio[] }`
   - Change export to: `export function ServicesSection({ servicios = [] }: ServicesSectionProps)`
   - Create an icon map object at module level:
     ```typescript
     import { Wrench, CheckCircle, Shield, Settings, Zap, type LucideIcon } from 'lucide-react'
     const iconMap: Record<string, LucideIcon> = {
       wrench: Wrench, 'check-circle': CheckCircle, shield: Shield,
       settings: Settings, zap: Zap,
     }
     ```
   - Replace the 3 hardcoded `<div>` cards with: `{servicios.map((s, i) => { const Icon = iconMap[s.icono] ?? Wrench; return (<div key={i} className="text-center"><Icon size={40} className="text-brand-red mx-auto" /><h3 className="mt-4 text-xl font-bold text-brand-text">{s.titulo}</h3><p className="mt-2 text-sm text-brand-text/60">{s.descripcion}</p></div>) })}`
   - Keep the section wrapper, heading ("Nuestros Servicios"), subtitle, and grid exactly as-is (same Tailwind classes: py-16, px-4, bg-brand-bg, max-w-6xl, mt-12, grid-cols-1 md:grid-cols-2 lg:grid-cols-3, gap-8)
   - Empty state: if servicios.length === 0, hide the grid div entirely (render only the section with heading and subtitle). This is a "silent" empty state per UI-SPEC.

3. Refactor `src/components/home/values-section.tsx`:
   - Add interface: `interface Valor { titulo: string; descripcion: string; icono: string }`
   - Add interface: `interface ValuesSectionProps { valores?: Valor[] }`
   - Change export to: `export function ValuesSection({ valores = [] }: ValuesSectionProps)`
   - Create icon map:
     ```typescript
     import { Award, Clock, Users, Shield, CheckCircle, type LucideIcon } from 'lucide-react'
     const iconMap: Record<string, LucideIcon> = {
       award: Award, clock: Clock, users: Users, shield: Shield, 'check-circle': CheckCircle,
     }
     ```
   - Replace hardcoded cards with map over valores prop (same pattern as services)
   - Keep exact Tailwind: bg-brand-surface, py-16, px-4, max-w-6xl, mt-12, grid-cols-1 md:grid-cols-2 lg:grid-cols-3, gap-8
   - Empty state: if valores.length === 0, render section with heading only, hide grid.

4. Refactor `src/components/home/metrics-counter.tsx`:
   - Add interface: `interface Metrica { valor: number; sufijo?: string; etiqueta: string }`
   - Add interface: `interface MetricsSectionProps { metricas?: Metrica[] }`
   - Change MetricsSection export to: `export function MetricsSection({ metricas = [] }: MetricsSectionProps)`
   - Replace the 4 hardcoded `<Counter>` calls with: `{metricas.map((m, i) => <Counter key={i} target={m.valor} suffix={m.sufijo ?? ''} label={m.etiqueta} />)}`
   - Keep the Counter sub-component EXACTLY as-is (the 'use client' directive, useRef, useEffect, IntersectionObserver animation — do NOT change any of this)
   - Keep exact Tailwind on section: py-16, px-4, bg-brand-surface, grid-cols-2 md:grid-cols-4, gap-8, max-w-4xl
   - Empty state: if metricas.length === 0, render section with empty grid (no counters shown).
   - IMPORTANT: The file must keep `'use client'` at the top because Counter uses hooks.
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && grep -q "HOMEPAGE_CONTENT_QUERY" src/sanity/queries/homepage.ts && grep -q "servicios" src/components/home/services-section.tsx && grep -q "ServicesSectionProps" src/components/home/services-section.tsx && grep -q "ValuesSectionProps" src/components/home/values-section.tsx && grep -q "MetricsSectionProps" src/components/home/metrics-counter.tsx && grep -q "iconMap" src/components/home/services-section.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/sanity/queries/homepage.ts exists with `export const HOMEPAGE_CONTENT_QUERY = defineQuery(`
    - src/sanity/queries/homepage.ts contains `servicios[]{titulo, descripcion, icono}`
    - src/sanity/queries/homepage.ts contains `metricas[]{valor, sufijo, etiqueta}`
    - src/components/home/services-section.tsx contains `interface Servicio`
    - src/components/home/services-section.tsx contains `servicios = []` default param
    - src/components/home/services-section.tsx contains `iconMap` with wrench, check-circle, shield, settings, zap
    - src/components/home/services-section.tsx does NOT contain hardcoded "Calibracion de Equipos" string
    - src/components/home/values-section.tsx contains `interface Valor`
    - src/components/home/values-section.tsx contains `valores = []` default param
    - src/components/home/values-section.tsx does NOT contain hardcoded "Experiencia Comprobada" string
    - src/components/home/metrics-counter.tsx contains `interface Metrica`
    - src/components/home/metrics-counter.tsx contains `metricas = []` default param
    - src/components/home/metrics-counter.tsx contains `'use client'` directive
    - src/components/home/metrics-counter.tsx does NOT contain hardcoded `target={200}`
  </acceptance_criteria>
  <done>All three homepage section components accept props with typed interfaces, render dynamically from arrays, use icon maps for Lucide components, show silent empty state when data is absent, and preserve exact existing Tailwind layout.</done>
</task>

<task type="auto">
  <name>Task 2: Wire homepage page.tsx to fetch from Sanity and pass props to section components</name>
  <files>src/app/(site)/page.tsx</files>
  <read_first>
    - src/app/(site)/page.tsx (current server component — must preserve existing banners + featuredEquipos fetch)
    - src/sanity/queries/homepage.ts (new query from Task 1 — verify export name)
    - src/components/home/services-section.tsx (verify prop interface after Task 1 refactor)
    - src/components/home/values-section.tsx (verify prop interface after Task 1 refactor)
    - src/components/home/metrics-counter.tsx (verify prop interface after Task 1 refactor)
  </read_first>
  <action>
Update `src/app/(site)/page.tsx`:

1. Add import: `import { HOMEPAGE_CONTENT_QUERY } from '@/sanity/queries/homepage'`

2. Extend the Promise.all to include a third fetch:
```typescript
const [banners, featuredEquipos, homepageContent] = await Promise.all([
  client.fetch(BANNERS_QUERY).catch(() => []),
  client.fetch(FEATURED_EQUIPOS_QUERY).catch(() => []),
  client.fetch(HOMEPAGE_CONTENT_QUERY).catch(() => null),
])
```

3. Update JSX to pass props:
```tsx
<HeroCarousel banners={banners} />
<ServicesSection servicios={homepageContent?.servicios ?? []} />
<ValuesSection valores={homepageContent?.valores ?? []} />
<FeaturedEquipment equipos={featuredEquipos} />
<MetricsSection metricas={homepageContent?.metricas ?? []} />
<ReclamacionesCta />
```

4. Keep all existing imports. Do NOT change the component order in JSX — it must remain: HeroCarousel, ServicesSection, ValuesSection, FeaturedEquipment, MetricsSection, ReclamacionesCta.
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && grep -q "HOMEPAGE_CONTENT_QUERY" src/app/\(site\)/page.tsx && grep -q "homepageContent" src/app/\(site\)/page.tsx && grep -q "servicios=" src/app/\(site\)/page.tsx && grep -q "valores=" src/app/\(site\)/page.tsx && grep -q "metricas=" src/app/\(site\)/page.tsx && npx playwright test tests/content.spec.ts --project chromium 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(site)/page.tsx contains `import { HOMEPAGE_CONTENT_QUERY } from '@/sanity/queries/homepage'`
    - src/app/(site)/page.tsx contains `client.fetch(HOMEPAGE_CONTENT_QUERY).catch(() => null)`
    - src/app/(site)/page.tsx contains `servicios={homepageContent?.servicios ?? []}`
    - src/app/(site)/page.tsx contains `valores={homepageContent?.valores ?? []}`
    - src/app/(site)/page.tsx contains `metricas={homepageContent?.metricas ?? []}`
    - src/app/(site)/page.tsx still contains `HeroCarousel`, `FeaturedEquipment`, `ReclamacionesCta` (existing components unchanged)
    - Playwright content tests pass: `npx playwright test tests/content.spec.ts --project chromium` exits 0
  </acceptance_criteria>
  <done>Homepage fetches servicios, valores, and metricas from Sanity via HOMEPAGE_CONTENT_QUERY, passes them as props to each section component, and existing Playwright tests still pass.</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/content.spec.ts --project chromium` passes (homepage sections render correctly — empty state or with data)
- All three components accept typed props and have no hardcoded content strings
- GROQ query projects exactly the fields defined in the schema
</verification>

<success_criteria>
- ServicesSection, ValuesSection, MetricsSection all accept props (no hardcoded content)
- Homepage page.tsx fetches from HOMEPAGE_CONTENT_QUERY and passes data to all sections
- Empty arrays render silent empty state (section heading visible, no grid/counters)
- Existing Playwright content tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/05-cms-completo-y-studio-en-espa-ol-para-edici-n-sin-c-digo/05-02-SUMMARY.md`
</output>
