---
phase: 03-content-pages-and-contact
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(site)/nosotros/page.tsx
  - src/components/layout/footer.tsx
autonomous: true
requirements:
  - INST-01
  - INST-02

must_haves:
  truths:
    - "Visitor can navigate to /nosotros and read company history and values"
    - "Footer on every page shows email, address, phone, social links, and navigation"
  artifacts:
    - path: "src/app/(site)/nosotros/page.tsx"
      provides: "Nosotros page with company history and values"
      contains: "Testing Calibrations"
    - path: "src/components/layout/footer.tsx"
      provides: "Enhanced footer with libro de reclamaciones link"
      contains: "libro-de-reclamaciones"
  key_links:
    - from: "src/app/(site)/nosotros/page.tsx"
      to: "/"
      via: "Renders within (site) layout with Navbar/Footer"
      pattern: "export default"
---

<objective>
Build the /nosotros institutional page and enhance the Footer component to include a link to the libro de reclamaciones and ensure all contact data fields render correctly.

Purpose: Institutional pages build trust with B2B visitors and the footer is the universal navigation/contact element.
Output: /nosotros page with company story and values, Footer enhanced with reclamaciones link.
</objective>

<execution_context>
@/Users/hugoyupanqui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hugoyupanqui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-pages-and-contact/03-UI-SPEC.md

<interfaces>
From src/app/(site)/layout.tsx:
```typescript
// Footer receives these props from layout, populated by SITE_SETTINGS_QUERY:
<Footer
  siteName={settings?.siteName ?? 'Testing Calibrations S.A.C.'}
  email={settings?.email ?? ''}
  address={settings?.address ?? ''}
  whatsappNumber={settings?.whatsappNumber ?? ''}
  navLinks={settings?.navLinks ?? []}
  socialLinks={settings?.socialLinks ?? []}
  footerText={settings?.footerText ?? ''}
/>
```

From src/components/layout/footer.tsx:
```typescript
interface FooterProps {
  siteName: string
  email: string
  address: string
  whatsappNumber: string
  navLinks: Array<{ label: string; href: string }>
  socialLinks: Array<{ platform: string; url: string }>
  footerText: string
}
export function Footer({...}: FooterProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /nosotros page with company history and values</name>
  <files>
    src/app/(site)/nosotros/page.tsx
  </files>
  <read_first>
    src/app/(site)/layout.tsx,
    src/app/globals.css,
    .planning/phases/03-content-pages-and-contact/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/app/(site)/nosotros/page.tsx` as a server component (no `'use client'`):

- Container: `max-w-4xl mx-auto px-4 py-12`
- Heading: `<h1 className="text-2xl font-bold text-brand-text">` with text "Sobre Testing Calibrations S.A.C."
- Content: static placeholder paragraphs (v1, not Sanity-driven per RESEARCH.md Open Question 3) with `leading-relaxed text-brand-text/80`:
  - Paragraph 1: Company introduction — Testing Calibrations S.A.C. es una empresa especializada en calibracion, verificacion y reparacion de equipos de medicion. Con mas de 10 anos de experiencia, brindamos servicios de alta calidad a empresas de diversos sectores industriales.
  - Paragraph 2: Mission — Nuestra mision es garantizar la precision y confiabilidad de los equipos de medicion de nuestros clientes, contribuyendo a la calidad de sus procesos y productos.
  - Paragraph 3: Vision — Ser la empresa lider en servicios de calibracion y metrologia en el Peru, reconocida por la excelencia de nuestro equipo tecnico y la satisfaccion de nuestros clientes.

- Values subsection: heading `<h2 className="text-xl font-bold text-brand-text mt-12 mb-6">` "Nuestros Valores"
- Grid of 3 value cards: `grid grid-cols-1 md:grid-cols-3 gap-6`
  - "Precision" — icon CheckCircle from lucide-react, description about accuracy commitment
  - "Integridad" — icon Shield from lucide-react, description about transparency
  - "Compromiso" — icon Users from lucide-react, description about client dedication
- Each value card: `bg-brand-surface p-6 rounded-lg`, icon `text-brand-red mb-4` size 32, title `text-lg font-bold text-brand-text`, description `text-sm text-brand-text/60 mt-2`
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(site)/nosotros/page.tsx exists
    - src/app/(site)/nosotros/page.tsx contains "Sobre Testing Calibrations"
    - src/app/(site)/nosotros/page.tsx contains "Nuestros Valores"
    - src/app/(site)/nosotros/page.tsx contains "Precision"
    - src/app/(site)/nosotros/page.tsx contains "Integridad"
    - src/app/(site)/nosotros/page.tsx contains "Compromiso"
    - src/app/(site)/nosotros/page.tsx contains "max-w-4xl"
    - src/app/(site)/nosotros/page.tsx does NOT contain `'use client'`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>/nosotros page renders company history, mission, vision, and 3 value cards with icons</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Footer with libro de reclamaciones link and verify all contact data renders</name>
  <files>
    src/components/layout/footer.tsx
  </files>
  <read_first>
    src/components/layout/footer.tsx,
    src/app/(site)/layout.tsx,
    .planning/phases/03-content-pages-and-contact/03-UI-SPEC.md
  </read_first>
  <action>
Modify `src/components/layout/footer.tsx` to add a "Libro de Reclamaciones" link in the navigation column:

1. In the "Navegacion" column (Column 2), after the `navLinks.map(...)` rendered list, add a hardcoded link:
```tsx
<li>
  <Link
    href="/libro-de-reclamaciones"
    className="text-brand-text/60 hover:text-brand-red text-sm transition-colors"
  >
    Libro de Reclamaciones
  </Link>
</li>
```
This link is NOT from siteSettings.navLinks — it is always present in the footer per Peruvian regulation.

2. In the Contact column (Column 1), add a label before each contact field for clarity:
- Before email: prefix with `Email: ` inside the `<p>` tag
- Before address: prefix with `Direccion: ` inside the `<p>` tag
- Before whatsappNumber: prefix with `WhatsApp: ` inside the `<p>` tag

3. Keep all existing props and interface unchanged — no breaking changes.
  </action>
  <verify>
    <automated>cd /Users/hugoyupanqui/rental && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/footer.tsx contains `libro-de-reclamaciones`
    - src/components/layout/footer.tsx contains `Libro de Reclamaciones`
    - src/components/layout/footer.tsx contains `Email:`
    - src/components/layout/footer.tsx contains `WhatsApp:`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Footer shows libro de reclamaciones link and labeled contact fields (email, address, whatsapp)</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /nosotros renders company history, values section with 3 cards
- Footer on every page includes "Libro de Reclamaciones" link
- Footer contact fields have descriptive labels
</verification>

<success_criteria>
- INST-01: /nosotros page shows company history and values
- INST-02: Footer shows contact data, address, social links, navigation, and reclamaciones link
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-pages-and-contact/03-02-SUMMARY.md`
</output>
